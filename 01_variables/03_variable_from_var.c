// a에 400을 대입하고 b에 a를 대입하여 a와 b를 각각 한 줄에 출력하시오.
#include <stdio.h>

int main() {
    int a;
    int b;
    // 두 개의 정수 변수 선언 (declaration)

    a = 400;
    b = a;
    // 두 개의 정수 변수에 각각 값 대입 (assignment)

    printf("%d\n%d\n", a, b);

    return 0;
}
