// 두 변수 a와 b에 300과 400을 할당한 후, 두 변수를 사용하여 "700"을 출력하시오.
#include <stdio.h>

int main() {
    int a;
    int b;
    // 두 개의 정수 변수 선언 (declaration)

    a = 300;
    b = 400;
    // 두 개의 정수 변수에 각각 값 대입 (assignment)

    printf("%d\n", a + b);

    return 0;
}
